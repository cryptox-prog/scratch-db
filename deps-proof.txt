Zero-dep verification instructions
=================================

Run the following commands locally to verify this repository has no third-party runtime
dependencies and that the built artifact links only system libraries (libc, libstdc++, pthread, etc.).

1) Quick scan for common third-party headers (should produce no matches):

   grep -R --line-number -E "#include <(boost|fmt|nlohmann|json.hpp|rapidjson|absl|sqlite3|openssl|yaml|zlib)" || true

2) Check CMakeLists for FetchContent/ExternalProject usage (should produce no matches):

   grep -n "FetchContent\|ExternalProject_Add" CMakeLists.txt || true

3) Build and test (captured output)

Run:

   cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure

Captured output (truncated to relevant sections):

-- Configuring done (0.0s)
-- Generating done (0.0s)
-- Build files have been written to: /home/pranav/Documents/programs/hcktn/zero-dep/scratch-db/build
ninja: no work to do.
Test project /home/pranav/Documents/programs/hcktn/zero-dep/scratch-db/build
         Start  1: PageTests
 1/12 Test  #1: PageTests ........................   Passed    0.00 sec
...
12/12 Test #12: TablePrinterTests ................   Passed    0.00 sec

100% tests passed out of 12

Total Test time (real) =   0.16 sec

4) Dynamic link inspection

Run:

   ldd build/db_cli

Captured output:

            linux-vdso.so.1 (0x00007fe4bf7b4000)
            libstdc++.so.6 => /usr/lib/libstdc++.so.6 (0x00007fe4bf200000)
            libm.so.6 => /usr/lib/libm.so.6 (0x00007fe4bf561000)
            libgcc_s.so.1 => /usr/lib/libgcc_s.so.1 (0x00007fe4bf534000)
            libc.so.6 => /usr/lib/libc.so.6 (0x00007fe4bee00000)
            /lib64/ld-linux-x86-64.so.2 => /usr/lib64/ld-linux-x86-64.so.2 (0x00007fe4bf7b6000)

This shows only system libraries (libstdc++, libc, libm, libgcc). No third-party runtime libraries are linked.

5) Third-party header scan (heuristic)

Run:

   grep -R --line-number -E "#include <(boost|fmt|nlohmann|json.hpp|rapidjson|absl|sqlite3|openssl|yaml|zlib)" || true

Captured output: (none)

6) Vendored file heuristics

Run:

   git ls-files | grep -E "(json.hpp|sqlite3|third_party|vendor|extern)" || true

Captured output: (none)

Notes for judges
----------------
- This is a C++ project that uses only the C++17 standard library and POSIX APIs (termios, open/fcntl,
   file I/O). The build is CMake-based; there are no package manifests like package.json, Cargo.toml,
   go.mod, or requirements.txt in the repository.
- The repository contains an in-repo test harness (`tests/test_utils.hpp`) instead of a third-party
   test framework.
- If you'd like, I can also commit the exact raw terminal output into this file (full logs) or paste
   it into a CI-friendly `deps-proof-raw.txt`. Tell me which you prefer.
